// datos.js
const variables = [];
const eventos =[]
export const agregarVariables = (key,value) => {
    variables.push({key,value});
};
export const agregarEventos = (nuevosDatos) => {
    datos.push(nuevosDatos);
  };
;
    
export const obtenerVariables = () => {
  return eventos;
};
export const obtenerEventos = () => {
    return eventos;
  };
